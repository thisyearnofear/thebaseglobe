import Cloud from"./Cloud";import{world}from"../../game";import{Colors}from"../utils/Colors";export class Sky{constructor(){this.mesh=new THREE.Object3D,this.nClouds=20,this.clouds=[];const o=2*Math.PI/this.nClouds,s=new THREE.Color(13148872);for(let i=0;i<this.nClouds;i++){const r=new Cloud(s);this.clouds.push(r);var t=o*i,h=world.seaRadius+150+200*Math.random();r.mesh.position.y=Math.sin(t)*h,r.mesh.position.x=Math.cos(t)*h,r.mesh.position.z=-300-500*Math.random(),r.mesh.rotation.z=t+Math.PI/2;const e=1+2*Math.random();r.mesh.scale.set(e,e,e),this.mesh.add(r.mesh)}}tick(o){for(var s=0;s<this.nClouds;s++)this.clouds[s].tick(o);this.mesh.rotation.z+=game.speed*o}}